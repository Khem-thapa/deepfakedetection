# Core dependencies
streamlit>=1.24.0  # For the web interface
pillow>=8.3.0     # For image processing
tensorflow>=2.5.0  # For loading and running models
numpy>=1.19.2     # For array operations

# Optional: Better CPU/GPU support for TensorFlow
# Comment out if not needed
tensorflow-io-gcs-filesystem>=0.23.1  # For TensorFlow I/O operations